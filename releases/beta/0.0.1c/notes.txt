Right now only recreation terminal mode is used so type "MODE" needs no change.

Same with type "ACCESSTYPE" is currently unused however future plans include charging vault dwellers money per minute for terminal usage or requiring a logon.
